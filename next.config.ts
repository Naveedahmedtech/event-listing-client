import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['source.unsplash.com', 'picsum.photos', 'fastly.picsum.photos', 'via.placeholder.com'],
  },
};

export default nextConfig;

'use client';

import React from 'react';
import RegistrationForm from '@/organisms/(auth)/RegistrationForm';

const RegistrationPage = () => {
    return (
        <RegistrationForm />
    );
};

export default RegistrationPage;

'use client';

import React from 'react';
import SignInForm from '@/organisms/(auth)/SignInForm';

const SigninPage = () => {
    return (
        <SignInForm />
    );
};

export default SigninPage;
